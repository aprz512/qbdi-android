{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the qtrace-ui desktop window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
